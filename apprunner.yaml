version: 1.0
runtime: python-3.11
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python -m gunicorn --chdir rival-backend -w 4 "src.main:app" --bind "0.0.0.0:8080"
  network:
    port: 8080