version: 1.0
runtime: python3.12
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python -m gunicorn --chdir rival-backend -w 4 -k uvicorn.workers.UvicornWorker "src.main:app" --bind "0.0.0.0:8080"
  network:
    port: 8080
